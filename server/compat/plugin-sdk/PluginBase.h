#pragma once

// The dedicated server only consumes plugin-sdk's fixed-layout data declarations.
// Keep injector, event, pattern-scanning, and RenderWare dependencies in the Windows client.
#include "Base.h"

#include <cstddef>
#include <cstdint>
#include <cstring>
